# Promote Implementation Plan

> **Status: Completed** — ready for PR into spike/planned-features

> **For agentic workers:** Read `docs/FEATURE-DEVELOPMENT.md`. Use `/launchpad-domain` for entity changes, `/launchpad-dev` for verification. Commit after each task with the message specified below.

**Goal:** Vertical slice for cross-env promote: new release with source artifact + process_snapshot, target-env config re-resolution, deploy job; API + CLI.

**Architecture:** Mirror `Rollback` via `enqueueRelease`. `Promote` resolves both envs, validates source release (succeeded + deployed to from), resolves config on **to**, enqueues standard deploy. No worker changes.

**Tech Stack:** Go, chi, cobra, SQLite/Postgres

**Spec:** `docs/superpowers/specs/2026-07-13-promote-design.md`

**Branch:** `feat/promote` (PR into `spike/planned-features`)

---

## Task 1: Spec/plan on branch

- [x] Commit spec + plan
- [x] Commit: `docs: add promote Wave 3 spec and implementation plan`

## Task 2: Service Promote + tests

**Files:**
- Modify: `internal/service/release_service.go`
- Modify: `internal/service/changeset_service_test.go` (or `release_promote_test.go`)

- [x] Implement `Promote`
- [x] Tests: config re-resolution, snapshot copy, rejects, default version
- [x] Verify: `mise exec -- go test ./internal/service/...`
- [x] Commit: `feat(service): implement cross-env promote with target config re-resolve`

## Task 3: API

**Files:**
- Modify: `internal/api/handlers.go`

- [x] Route + handler
- [x] Verify: `mise exec -- go test ./...`
- [x] Commit: `feat(api): add POST /v1/projects/{project}/promote`

## Task 4: CLI and client

**Files:**
- Modify: `pkg/apiclient/client.go`
- Modify: `internal/cli/root.go`

- [x] Client `Promote` + CLI command
- [x] Verify: `mise exec -- make build`
- [x] Commit: `feat(cli): add launchpad promote command`

## Task 5: Docs cleanup

- [x] DOMAIN shipped API/CLI; DX-VISION mark Promote shipped/in-progress appropriately
- [x] README example if needed
- [x] Full verification
- [x] Commit: `docs: mark promote as shipped on spike`

---

## Final verification

```bash
mise exec -- make test
mise exec -- make build
mise exec -- go vet ./...
```
